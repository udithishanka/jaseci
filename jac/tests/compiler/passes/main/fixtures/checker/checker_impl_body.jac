"""Test type checking in impl bodies (function, archetype, enum)."""

# Function declaration
def process_data;

# Archetype declaration
obj DataProcessor {
    def process(val: int) -> int;
}

# Enum declaration
enum Status;
