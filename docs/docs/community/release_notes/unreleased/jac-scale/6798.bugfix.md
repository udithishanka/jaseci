- **Fix: `--no-image` pods get the scale plugin's runtime deps, a startup probe, and a stub jac.toml**: the in-pod bootstrap installed bare `jac-scale` (so the `jac-scale:scale` plugin failed to load - gateway crashed, services 404'd on `/healthz/*`); it now installs `jac-scale[all]` + `requests` (a runtime dep jac-scale ships only in its `test` extra) + `jac-client` when present. No-image pods also get a startup probe (the multi-minute in-pod install/compile would be liveness-SIGKILLed otherwise) and the bootstrap writes a minimal `[project]` jac.toml (the real one is excluded as a secret, but `jac start <svc>.jac` requires a project jac.toml to exist).
