- **Fix: Type Checker Resolves `root()` Builtin**: `root()` is now declared as an ambient builtin returning `Root` in `jac_builtins.pyi`, eliminating false W1051 (Unknown type) warnings on `root()` calls and downstream expressions like `root() spawn Walker()`.
